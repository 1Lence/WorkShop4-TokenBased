Сервис соответствует ДЗ 4 + ДЗ 5

Обоснование выбора подхода с подписью и шифрованием данных JWE:
- Шифрует чувствительные данные, которые невозможно прочитать без ключа;
- Незащищенный канал, проблема так же решается шифрованием, но стоило бы подключить HTTPS (по тз я не понял нам нужно
было это реализовывать или нет);
- Компрометация, JWE гарантирует, что данные увидит только тот, кто владеет ключом и знает верный алгоритм.
- Подмена, в случае если всё же кто-то сможет расшифровать данные, то благодаря алгоритму A256GCM этого сделать будет 
нельзя, поскольку, происходит побитовая сверка и отклонение даже на 1 бит вызовет ошибку.

Изменения по сравнению с Дз4:
- Добавлен Swagger;
- Изменена обработка исключений (Теперь данные выводятся красивым Json-ом);
- Добавлен контракт для мапперов;
- Jwt заменен на Jwe;
- Данные для подключения к бд не стал выносить из yml, по причине того что это учебный проект и проверяющему достаточно
будет нажать 2 (docker-compose, application.yml) "кнопки" для быстрого подключения к бд.
- Добавил демонстрацию умения использования ENV как переменных, ключи добавляются только через переменные окружения,
.env добавлен в .gitingore.

Для проверяющего, если не захочется генерировать ключ (для проверки можно использовать один и тот же):
Ключ: 5QkX1qKp3iPh4mfrc5D0F3eG9HvA2BcD4Efg

UPD: На тесты времени нет, хочется осознанно до-пройти курс, а не в спешке. Todo: это то что оставил на потом.

Скрин как теперь выглядят ошибки:
![Postman.png](screen%2FPostman.png)