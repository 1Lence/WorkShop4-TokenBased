volumes:
  workshop_db:

services:
  workshop_db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=workshop_db
    volumes:
      - workshop_db:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5434:5432"